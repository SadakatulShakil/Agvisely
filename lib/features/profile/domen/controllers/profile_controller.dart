import 'package:get/get.dart';

/// Profile — screen state. Wire repositories + observables here.
class ProfileController extends GetxController {
  final isLoading = false.obs;

  @override
  void onInit() {
    super.onInit();
    // TODO: load Profile data
  }
}
